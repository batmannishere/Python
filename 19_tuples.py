'''
TUPLES
A built in data type that lets us create immutable sequence of value
SYNTAX
nameoftuple=()
TUPLE METHODS
nameoftuple.count(element): Returns the number of occurrences of a specified element in the tuple. 
nameoftuple.index(element, start, end): Returns the index of the first occurrence of a specified element; optional start and end parameters allow searching within a specific range.
len(nameoftuple): Returns the number of elements. 
min(nameoftuple) and max(nameoftuple): Return the smallest and largest elements, respectively. 
sorted(nameoftuple): Returns a new list with elements sorted in ascending order (note: it returns a list, not a tuple). 
nameoftuple.(iterable): Converts an iterable (like a list or string) into a new tuple.
'''